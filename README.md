Notas:
El archivo hosts, en este caso utiliza ~/.ssh/config para no poner toda la info de los hosts.

Uso:
./nscp <folder origen> <folder destino>
./nssh <comando>

./nss <script> #va a enviar el script a los nodos en hosts y ejecutarlo en cada uno.
